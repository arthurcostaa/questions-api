[project]
name = "questions-api"
version = "0.1.0"
description = "A simple REST API for solving questions"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.4",
    "python-dotenv>=1.1.1",
]
